export interface Order {
  id?: string;
  clientId?: string;
  name: string;
  userId?: string;
  address: string;
  productId?: string;
  product: string;
  imageProd?: string;
  imageClient?: string;
}